const city: string = 'Bagerhat';
const fruits: string[] = ['helal', 'kalam', 'jalal'];


/* objects types */
const students: { name: string, age: number, roll: number } = {
    name: 'Helal Uddin',
    age: 25,
    roll: 10
}

/* another object type */
type Human = { name: string, age: number, roll: number };

const man: Human = {
    name: 'helal',
    age: 60,
    roll: 20
}