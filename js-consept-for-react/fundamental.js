const myName = 'Helal Uddin';
let job = 'marketting';
job = 'Developer';


const numbers = [15, 54, 84, 20, 10, 2, 0];
numbers.length;

//  for loop
for (let i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
}

// object
const students = {
    name: 'Helal Uddin',
    age: 25,
    roll: 10,
    homeCity: 'Chitalmari'
}

const studentAge = 'age';
console.log(students[studentAge]);

